apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: intent-allow-gateway-only
  namespace: {{ .Values.namespace }}
spec:
  podSelector:
    matchLabels:
      app: intent            
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: gateway  