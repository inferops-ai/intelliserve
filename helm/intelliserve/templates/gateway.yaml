apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gateway
  namespace: {{ .Values.namespace }}
spec:
  gatewayClassName: nginx
  listeners:
  - name: http
    port: {{ .Values.gatewayAPI.port }}
    protocol: HTTP
    hostname: {{ .Values.hostname }}