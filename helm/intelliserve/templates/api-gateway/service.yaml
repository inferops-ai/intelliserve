apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: gateway
  ports:
    - port: {{ .Values.gatewayAPI.port }}
      targetPort: {{ .Values.gatewayAPI.targetPort }}