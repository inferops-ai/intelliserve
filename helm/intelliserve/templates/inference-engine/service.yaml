apiVersion: v1
kind: Service
metadata:
  name: inference-service
  namespace: {{ .Values.namespace }}
spec:
  selector: 
    app: inference
  ports:
  - port: {{ .Values.inferenceEngine.port }}
    targetPort: {{ .Values.inferenceEngine.targetPort }}