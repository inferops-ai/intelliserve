apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: inference-allow-gateway-only
  namespace: {{ .Values.namespace }}
spec:
  podSelector:
    matchLabels:
      app: inference            
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: gateway  